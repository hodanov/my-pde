name: CI nvim-sync
permissions:
  contents: read
  pull-requests: write

on:
  pull_request:
    paths:
      - "scripts/nvim-sync/**"
      - mise.toml

jobs:
  ci:
    uses: ./.github/workflows/go_module_ci.yml
    with:
      module: nvim-sync
