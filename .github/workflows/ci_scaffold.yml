name: CI scaffold
permissions:
  contents: read
  pull-requests: write

on:
  pull_request:
    paths:
      - "scripts/scaffold/**"
      - .github/workflows/go_module_ci.yml
      - mise.toml

jobs:
  ci:
    uses: ./.github/workflows/go_module_ci.yml
    with:
      module: scaffold
